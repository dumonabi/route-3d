# Route 3D

Origin, destination, transport mode, and a Google Maps 3D route preview.

## Setup

Enable **Maps JavaScript API**, **Routes API**, and **Places API (New)** in Google Cloud.

```bash
cp .env.local.example .env.local
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=...
npm install
npm run dev
```
